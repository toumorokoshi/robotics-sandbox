load("@rules_rust//rust:defs.bzl", "rust_binary", "rust_test")
load("@crates//:defs.bzl", "all_crate_deps")

rust_binary(
    name = "kalmann_filter",
    srcs = ["src/main.rs"],
    edition = "2021",
    deps = all_crate_deps(
        normal = True,
    ),
)

rust_test(
    name = "kalmann_filter_test",
    crate = ":kalmann_filter",
    deps = all_crate_deps(
        normal_dev = True,
    ),
)
